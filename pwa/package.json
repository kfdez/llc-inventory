{
  "name": "llc-inventory-v2-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/copy-wasm.js",
    "build": "vite build",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run",
    "test": "node --test"
  },
  "dependencies": {
    "zxing-wasm": "^3.0.2"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
